{
  "description": "Native operation hints in their decided v4 spellings: a single-member wrapper, empty unless the hint carries a payload.",
  "examples": {
    "arithmetic": { "Arithmetic": {} },
    "comparison": { "Comparison": {} },
    "stringOp": { "StringOp": {} },
    "collectionOp": { "CollectionOp": {} },
    "platformSpecificWasm": { "PlatformSpecific": { "platform": "wasm" } },
    "platformSpecificJavaScript": { "PlatformSpecific": { "platform": "javascript" } }
  }
}