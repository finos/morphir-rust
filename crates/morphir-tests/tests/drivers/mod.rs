//! Test drivers for the Cucumber acceptance suite.
//!
//! A driver wraps the system under test and exposes domain-specific methods
//! that step definitions delegate to, keeping setup and assertions out of the
//! step bindings.

pub mod config_driver;
